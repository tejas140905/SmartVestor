{
  "name": "smartvestor",
  "version": "1.0.0",
  "description": "AI-powered investment advisory platform for the Indian market",
  "main": "backend/server.js",
  "scripts": {
    "server": "cd backend && nodemon server.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "cd backend && node server.js",
    "install-all": "npm install && cd backend && npm install && cd ../client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["investment", "finance", "india", "ai", "advisory"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  }
}
